# Adopted Conventions — what the best practitioners actually do

_Auto-generated by `crawlers/gen_conventions.py`. **Empirical**, not prescriptive: convention-adoption among the **19 top repos** (median ≥ 85, ≥ 10 skills; 1598 skills) vs everyone else (3355 skills)._

| Convention | Best practitioners | Everyone else |
|---|---|---|
| `name` matches the folder slug | **99%** | 64% |
| Worked code example in body | **85%** | 61% |
| Delegates depth to a `reference/` dir | **66%** | 35% |
| Ships a `scripts/` dir | **48%** | 28% |
| Declares a `license` key | **42%** | 14% |
| Uses a `metadata` block | **41%** | 11% |
| Numbered procedure steps | **72%** | 75% |
| `allowed-tools` key (inverted!) | **5%** | 22% |
| Carries `origin`/`source` provenance keys | **0%** | 20% |

## What the top repos converged on

1. **`name` always equals the directory slug** — the sharpest distinguisher (near-universal among the best, ~⅔ elsewhere).
2. **Show, don't just tell** — a worked code example, not just headings.
3. **Progressive disclosure as a folder convention** — `reference/` for prose depth, `scripts/` for executable helpers (~2× the rest).
4. **Tidy, complete frontmatter** — `name` / `description` / `license` / `metadata`, and little else.

## The originator-vs-copier tell

Frontmatter keys each group sprouts:

- **Best practitioners:** `name`, `description`, `license`, `metadata`, `version`
- **Everyone else:** `name`, `description`, `version`, `allowed-tools`, `origin`, `license`

The lower tier carries **`origin`/`source`** keys — copy-provenance breadcrumbs that mark aggregated content.

> **On `allowed-tools` (the inverted row).** Don't read this as a quality negative. `allowed-tools` restricts which tools a skill may invoke — a real safety/scoping feature. It is rare among the elite because the canonical repos write *capability* skills (create documents, design, art) that need broad access. It is common in the rest because **workflow / agent / command** skills (research pipelines, code-ops, slash-commands) restrict tools *on purpose* — and for those it is good practice. A high-quality repo, `flonat/claude-research` (median 85), uses it on 96% of its skills. The rule is: **declare `allowed-tools` when your skill takes actions; omit it when it produces content.**

## Caveat

Mild circularity: "best practitioners" is cut by the rubric's own median. But several signals here — `metadata` adoption, worked examples, the `origin`/`source` contrast, the inverted `allowed-tools` — aren't directly scored, so they independently corroborate a real house style.

## The best-practitioner repos

- `Jeffallan/claude-skills` — median 95.5, 66 skills (marketplace)
- `JimLiu/baoyu-skills` — median 95.5, 22 skills (domain-pack)
- `openai/skills` — median 91.5, 44 skills (canonical-reference)
- `alirezarezvani/claude-skills` — median 90.5, 345 skills (mega-collection)
- `freshtechbro/claudedesignskills` — median 90.5, 90 skills (marketplace)
- `foxj77/claude-code-skills` — median 90.0, 19 skills (domain-pack)
- `borghei/claude-skills` — median 90.0, 340 skills (mega-collection)
- `daymade/claude-code-skills` — median 90.0, 68 skills (marketplace)
- `coreyhaines31/marketingskills` — median 89.5, 45 skills (domain-pack)
- `anthropics/skills` — median 88.8, 18 skills (canonical-reference)
- `posit-dev/skills` — median 88.2, 25 skills (canonical-reference)
- `julianobarbosa/claude-code-skills` — median 88.0, 123 skills (marketplace)
- `wshobson/agents` — median 87.5, 156 skills (marketplace)
- `QuestForTech-Investments/claude-code-skills` — median 86.2, 24 skills (canonical-reference)
- `AgriciDaniel/claude-seo` — median 86.2, 33 skills (domain-pack)
- `dmccreary/claude-skills` — median 86.2, 26 skills (domain-pack)
- `deanpeters/Product-Manager-Skills` — median 85.5, 54 skills (domain-pack)
- `flonat/claude-research` — median 85.0, 50 skills (domain-pack)
- `johnlindquist/claude` — median 85.0, 50 skills (domain-pack)
